const fs = require("fs"); const path = require("path"); const sharp = require("sharp"); const inputDir = path.join(__dirname, "..", "public", "cards"); const outputDir = path.join(__dirname, "..", "public", "cards", "webp"); if (!fs.existsSync(outputDir)) { fs.mkdirSync(outputDir, { recursive: true }); } const files = fs.readdirSync(inputDir) .filter(file => /\.png$/i.test(file)); async function convert() { console.log(`Trovate ${files.length} immagini PNG.`); console.log(""); let converted = 0; let skipped = 0; for (const file of files) { const input = path.join(inputDir, file); const output = path.join( outputDir, file.replace(/\.png$/i, ".webp") ); if (fs.existsSync(output)) { console.log(`SKIP: ${file}`); skipped++; continue; } try { await sharp(input) .resize(600, 840, { fit: "fill" }) .webp({ quality: 82 }) .toFile(output); console.log(`OK: ${file}`); converted++; } catch (error) { console.error(`ERRORE: ${file}`); console.error(error.message); } } console.log(""); console.log("================================="); console.log("CONVERSIONE COMPLETATA"); console.log(`Convertite: ${converted}`); console.log(`Saltate: ${skipped}`); console.log("================================="); } convert();